{{- if .Values.config.secrets }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    app: {{ template "cf-authz-web-api-docker.name" . }}
    chart: {{ template "cf-authz-web-api-docker.chart" . }}
    component: "{{ template "cf-authz-web-api-docker.name" . }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "cf-authz-web-api-docker.fullname" . }}-senv
data:
  {{- range $key, $value := .Values.config.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end }}
