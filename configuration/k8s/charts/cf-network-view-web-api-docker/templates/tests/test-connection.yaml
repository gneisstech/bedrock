apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cf-network-view-web-api-docker.fullname" . }}-test-connection"
  labels:
    {{- include "cf-network-view-web-api-docker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cf-network-view-web-api-docker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
