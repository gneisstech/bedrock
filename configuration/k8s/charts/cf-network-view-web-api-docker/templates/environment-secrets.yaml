{{- if .Values.config.secrets }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    app: {{ template "cf-network-view-web-api-docker.name" . }}
    chart: {{ template "cf-network-view-web-api-docker.chart" . }}
    component: "{{ template "cf-network-view-web-api-docker.name" . }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "cf-network-view-web-api-docker.fullname" . }}-senv
data:
  {{- range $key, $value := .Values.config.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end }}
