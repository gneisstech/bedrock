{{- if .Values.config.environment }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ template "cf-network-view-web-api-docker.name" . }}
    chart: {{ template "cf-network-view-web-api-docker.chart" . }}
    component: "{{ template "cf-network-view-web-api-docker.name" . }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "cf-network-view-web-api-docker.fullname" . }}-env
data:
{{ tpl (toYaml .Values.config.environment) . | indent 2 }}
{{- end }}
