apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-react-app-docker.fullname" . }}
  labels:
    {{- include "cf-react-app-docker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-react-app-docker.selectorLabels" . | nindent 4 }}
