apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cf-react-app-docker.fullname" . }}-test-connection"
  labels:
    {{- include "cf-react-app-docker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cf-react-app-docker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
