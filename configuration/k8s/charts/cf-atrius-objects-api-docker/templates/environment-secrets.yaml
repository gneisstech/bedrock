{{- if .Values.config.secrets }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    app: {{ template "cf-atrius-objects-api-docker.name" . }}
    chart: {{ template "cf-atrius-objects-api-docker.chart" . }}
    component: "{{ template "cf-atrius-objects-api-docker.name" . }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "cf-atrius-objects-api-docker.fullname" . }}-senv
data:
  {{- range $key, $value := .Values.config.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end }}
