apiVersion: v1
kind: Service
metadata:
  name: {{ include "oauth-proxy-docker.fullname" . }}
  labels:
    {{- include "oauth-proxy-docker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oauth-proxy-docker.selectorLabels" . | nindent 4 }}
