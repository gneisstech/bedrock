apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-mod-security.fullname" . }}-test-connection"
  labels:
    {{- include "test-mod-security.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-mod-security.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
