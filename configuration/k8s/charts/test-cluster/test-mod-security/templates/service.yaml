apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-mod-security.fullname" . }}
  labels:
    {{- include "test-mod-security.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-mod-security.selectorLabels" . | nindent 4 }}
