apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cf-sql-server.fullname" . }}-test-connection"
  labels:
    {{- include "cf-sql-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cf-sql-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
