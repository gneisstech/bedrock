apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-elm-web-api-docker.fullname" . }}
  labels:
    {{- include "cf-elm-web-api-docker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-elm-web-api-docker.selectorLabels" . | nindent 4 }}
