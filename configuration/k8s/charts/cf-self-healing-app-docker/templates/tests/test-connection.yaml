apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cf-self-healing-app-docker.fullname" . }}-test-connection"
  labels:
    {{- include "cf-self-healing-app-docker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cf-self-healing-app-docker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
